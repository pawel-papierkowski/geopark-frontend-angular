/** Mockup. Will be replaced with other environment file as configured in angular.json. */
import { ProjectEnv } from "@/shared/config/types";

/** Environment variables for MOCK environment. */
export const environment: ProjectEnv = {
  production: false,
  build: 'MOCK',
  version: '0.0.0',
  apiUrl: '',
};
