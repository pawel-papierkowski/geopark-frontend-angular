# TODO

## Main task
Finish designing app and determining what we need for required functionality.

## Other things to do
- Create basic placeholder pages for all three parts (user, dev and admin).
- Use separate header and footer component. Fill footer correctly. Version will need environment.
- Additional necessary configuration for Angular project.
  - Running tests in IDE (right now it does not work)
  - Path alias (vue-style)
  - Environment
- Create workflow for this project so it deploys to GitHub pages.
- Configure app for github pages, so it responds to http://localhost:4200/geopark-frontend-angular/ instead of http://localhost:4200/.

## Later
- Add translation services and code needed to properly handle it (determining it, saving/loading from storage etc).
- Install and configure PlayWright.
